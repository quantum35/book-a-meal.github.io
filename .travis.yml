language: python

python:
  - "3.6"

install:
  - pip install -r requirements.txt
  - cd API
  - ls -la 
  
  - pip install coveralls

script:
  # - python test_app.py
  - python test_endpoints.py --cov-report term-missing --cov=resources
  # - python -m pytest --cov-report term-missing --cov=resources

after_success:
  - coveralls

notifications:
  email: false