language: python

python:
  - "3.6"

install:
  - pip install -r requirements.txt

services:
  - postgresql

before_script:
  - source ./.travis.sh
  - psql -c 'create database testchal_Booka_meal;' -U postgres

script: 

- cd api
- ls la
- cd test 
- ls -la

#  - nosetests --with-coverage --cover-package api
- nosetests test_authentication.py
- nosetests test_meals.py
- nosetests test_menu.py
- nosetests test_orders.py

after_success:

  - coveralls